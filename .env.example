POSTGRES_USER=giftswap
POSTGRES_PASSWORD=giftswap
POSTGRES_DB=giftswap
POSTGRES_PORT=5432

# Host-run API and goose (pnpm db:migrate). Inside Compose the host is `db`.
DATABASE_URL=postgres://giftswap:giftswap@localhost:5432/giftswap?sslmode=disable
